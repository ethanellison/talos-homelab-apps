apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cluster-default
  namespace: default
spec:
  instances: 3
  storage:
    size: 10Gi
  managed:
    roles:
      - name: coder
        ensure: present
        comment: Coder
        passwordSecret:
         name: coder-creds
