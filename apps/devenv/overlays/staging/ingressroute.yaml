apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ttyd
  namespace: devenv
spec:
  entryPoints:
    - web
  routes:
    - match: "Host(`dev.${STAGING_LB_IP}.nip.io`)"
      kind: Rule
      services:
        - name: ttyd
          port: 7681
